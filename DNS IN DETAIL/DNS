DNS (Domain Name System) is the phonebook of the internet. It translates human-friendly domain names (like google.com) into machine-friendly IP addresses (like 142.250.190.46) so routers can deliver your packets.

Without DNS, you would have to memorize a string of random numbers for every website you visit instead of typing names.
